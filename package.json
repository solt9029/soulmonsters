{
  "name": "soulmonsters",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": "22.14.0",
    "yarn": "1.22.22"
  },
  "scripts": {
    "build": "concurrently \"yarn workspace soulmonsters-server build\" \"yarn workspace soulmonsters-client build\"",
    "format": "yarn workspace soulmonsters-client format && yarn workspace soulmonsters-server format",
    "test": "yarn workspace soulmonsters-server test",
    "generate-graphql-types": "yarn workspace soulmonsters-client generate-graphql-types && yarn workspace soulmonsters-server generate-graphql-types",
    "dev": "concurrently \"yarn workspace soulmonsters-server start:dev\" \"yarn workspace soulmonsters-client dev\" \"yarn workspace soulmonsters-client dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
